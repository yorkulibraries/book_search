<%= form_with(model: [:admin, patron], local: true) do |form| %>
  <% if patron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patron.errors.count, "error") %> prohibited this patron from being saved:</h2>

      <ul>
      <% patron.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :patron_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :patron_email %>
  </div>

  <div class="field">
    <%= form.label :login_id %>
    <%= form.text_field :login_id, id: :patron_login_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
